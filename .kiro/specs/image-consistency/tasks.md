# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Fault Condition** - 角色外观 Prompt 注入缺失
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: For any scene where speaker is a character with `appearance` field in `characters.yaml`, generate random appearance strings and verify `build_sd_prompt()` includes them in the positive prompt
  - Create test file `tests/test_image_consistency_exploration.py`
  - Construct test scenes with speaker set to character names (e.g., "林舟", "苏晚") that have `appearance` fields
  - Mock `characters.yaml` data with appearance fields: e.g., `{"林舟": {"voice_type": "BV123_streaming", "appearance": "young man, short dark hair, black jacket"}}`
  - Call `build_sd_prompt(scene, styles_config, style_name, characters_config)` with the characters config
  - Assert the returned positive prompt contains the character's appearance description
  - Assert appearance is positioned after style prefix and before visual prompt
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (confirms bug exists - current `build_sd_prompt()` does not accept `characters_config` param and does not inject appearance)
  - Document counterexamples: e.g., `build_sd_prompt()` returns prompt without any appearance keywords for character scenes
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - 非角色场景及 Fallback 行为不变
  - **IMPORTANT**: Follow observation-first methodology
  - Create test file `tests/test_image_consistency_preservation.py`
  - **Observation Phase** (run on UNFIXED code):
    - Observe: `build_sd_prompt(narrator_scene, styles, style_name)` output for narrator scenes
    - Observe: `build_sd_prompt(no_appearance_scene, styles, style_name)` output for characters without `appearance` field
    - Observe: `generate_placeholder_image()` output when SD is unavailable
    - Observe: `generate_image_sd()` payload structure (no `alwayson_scripts`)
  - **Property-Based Tests**:
    - For all scenes where speaker is "narrator", `build_sd_prompt()` output with and without `characters_config` must be identical (prompt = style_prefix + visual_prompt + shot + emotion)
    - For all scenes where speaker has no `appearance` field in characters config, `build_sd_prompt()` output must be identical to original function output
    - For all scenes when SD is unavailable, `generate_all_images()` produces placeholder images regardless of appearance/IP-Adapter config
    - When `pipeline.yaml` has no `ip_adapter` config, `generate_image_sd()` payload must not contain `alwayson_scripts` field
    - Existing prompt parts (style prefix, visual prompt, shot type, emotion) maintain original order and content
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 3. Fix for 图片生成角色外观一致性

  - [x] 3.1 Add `appearance` field to `config/characters.yaml`
    - Add `appearance` field to 林舟: `"young man, short dark hair, black jacket, white shirt, tall and slender"`
    - Add `appearance` field to 苏晚: `"young woman, long black hair, white dress, slender figure, gentle eyes"`
    - Do NOT add `appearance` to narrator (旁白不需要外观描述)
    - Field is optional - existing characters without it should continue to work
    - _Bug_Condition: isBugCondition(scene) where scene.speaker has appearance in characters.yaml but prompt lacks it_
    - _Expected_Behavior: characters.yaml provides appearance data for prompt injection_
    - _Preservation: Characters without appearance field continue to work unchanged_
    - _Requirements: 1.3, 2.3_

  - [x] 3.2 Add `ip_adapter` config block to `config/pipeline.yaml`
    - Add `ip_adapter` section under `image` config with fields: `enabled` (false), `model`, `weight`, `reference_dir`
    - Default: `enabled: false`, `model: "ip-adapter-plus_sd15.safetensors"`, `weight: 0.6`, `reference_dir: "assets/reference_images"`
    - _Bug_Condition: pipeline.yaml lacks IP-Adapter configuration_
    - _Expected_Behavior: pipeline.yaml supports ip_adapter config block_
    - _Preservation: When ip_adapter.enabled is false or section missing, behavior unchanged_
    - _Requirements: 1.4, 2.4, 3.4_

  - [x] 3.3 Modify `build_sd_prompt()` in `scripts/03_generate_images.py`
    - Add `characters_config=None` parameter to function signature (backward compatible)
    - When `characters_config` is not None and `scene["speaker"]` is not "narrator":
      - Look up speaker in characters_config
      - If character exists and has `appearance` field, inject it into prompt parts
    - Injection position: after style prefix, before visual prompt
    - New prompt order: `style_prefix + [appearance] + visual_prompt + shot + emotion`
    - When characters_config is None or speaker is narrator or character has no appearance: behavior identical to original
    - _Bug_Condition: isBugCondition(scene) where appearance NOT IN buildCurrentPrompt(scene)_
    - _Expected_Behavior: appearance IN positive_prompt AND index(style_prefix) < index(appearance) < index(visual_prompt)_
    - _Preservation: narrator scenes and no-appearance characters produce identical output_
    - _Requirements: 1.2, 2.1, 2.2, 2.3, 3.1, 3.2, 3.5_

  - [x] 3.4 Modify `generate_image_sd()` in `scripts/03_generate_images.py`
    - Add `ip_adapter_config=None` and `reference_image_path=None` parameters
    - When IP-Adapter is enabled and reference_image_path exists:
      - Read reference image, base64 encode it
      - Add `alwayson_scripts` field to payload with ControlNet/IP-Adapter extension config
      - Include model name and weight from ip_adapter_config
    - When IP-Adapter is disabled or no reference image: payload identical to original (pure txt2img)
    - _Bug_Condition: generate_image_sd() does not support IP-Adapter reference image injection_
    - _Expected_Behavior: payload includes alwayson_scripts with IP-Adapter config when enabled_
    - _Preservation: When ip_adapter disabled or missing, payload unchanged from original_
    - _Requirements: 1.4, 2.4, 3.4_

  - [x] 3.5 Modify `generate_all_images()` in `scripts/03_generate_images.py`
    - Add `load_characters()` helper function to load `config/characters.yaml`
    - Load characters config at function start
    - Pass `characters_config` to `build_sd_prompt()` calls
    - Load IP-Adapter config from pipeline.yaml `image.ip_adapter` section
    - When IP-Adapter enabled, look up reference image for scene speaker in `reference_dir` (e.g., `assets/reference_images/林舟.png`)
    - Pass `ip_adapter_config` and `reference_image_path` to `generate_image_sd()` calls
    - Fallback logic (SD unavailable) remains unchanged - no appearance/IP-Adapter logic affects placeholder generation
    - _Bug_Condition: generate_all_images() does not load characters or pass appearance info_
    - _Expected_Behavior: characters loaded, appearance passed to build_sd_prompt, IP-Adapter config passed to generate_image_sd_
    - _Preservation: SD unavailable fallback, narrator scenes, no-appearance characters all unchanged_
    - _Requirements: 1.1, 1.2, 1.4, 2.1, 2.2, 2.4, 3.3, 3.4_

  - [x] 3.6 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - 角色外观 Prompt 注入
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - _Requirements: 2.1, 2.2, 2.3_

  - [x] 3.7 Verify preservation tests still pass
    - **Property 2: Preservation** - 非角色场景及 Fallback 行为不变
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix (no regressions)

- [x] 4. Checkpoint - Ensure all tests pass
  - Run full test suite to verify all exploration and preservation tests pass
  - Verify `build_sd_prompt()` correctly injects appearance for character scenes
  - Verify `build_sd_prompt()` produces identical output for narrator and no-appearance scenes
  - Verify `generate_image_sd()` includes IP-Adapter payload when enabled, unchanged when disabled
  - Verify `generate_all_images()` loads characters and passes config correctly
  - Ensure all tests pass, ask the user if questions arise
